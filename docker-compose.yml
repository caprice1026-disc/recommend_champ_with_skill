services:
  lol-skill-lab:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    volumes:
      - skill-lab-wrangler-state:/app/.wrangler

volumes:
  skill-lab-wrangler-state:
